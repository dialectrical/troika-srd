#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../rxjs-report-usage/bin/rxjs-report-usage" "$@"
  ret=$?
else 
  node  "$basedir/../rxjs-report-usage/bin/rxjs-report-usage" "$@"
  ret=$?
fi
exit $ret
