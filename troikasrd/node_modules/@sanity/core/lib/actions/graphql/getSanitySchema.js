"use strict";

var mockBrowserEnvironment = require('../../util/mockBrowserEnvironment');

function getSanitySchema(basePath) {
  var cleanup = mockBrowserEnvironment(basePath);

  var schemaMod = require('part:@sanity/base/schema');

  var schema = schemaMod.default || schemaMod;
  cleanup();
  return schema;
}

module.exports = getSanitySchema;