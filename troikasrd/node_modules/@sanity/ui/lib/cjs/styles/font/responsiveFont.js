var __create = Object.create;
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __getProtoOf = Object.getPrototypeOf;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __markAsModule = (target) => __defProp(target, "__esModule", { value: true });
var __export = (target, all) => {
  __markAsModule(target);
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __reExport = (target, module2, desc) => {
  if (module2 && typeof module2 === "object" || typeof module2 === "function") {
    for (let key of __getOwnPropNames(module2))
      if (!__hasOwnProp.call(target, key) && key !== "default")
        __defProp(target, key, { get: () => module2[key], enumerable: !(desc = __getOwnPropDesc(module2, key)) || desc.enumerable });
  }
  return target;
};
var __toModule = (module2) => {
  return __reExport(__markAsModule(__defProp(module2 != null ? __create(__getProtoOf(module2)) : {}, "default", module2 && module2.__esModule && "default" in module2 ? { get: () => module2.default, enumerable: true } : { value: module2, enumerable: true })), module2);
};
__export(exports, {
  fontSize: () => fontSize,
  responsiveFont: () => responsiveFont
});
var import_helpers = __toModule(require("../helpers"));
function responsiveFont(fontKey, props) {
  const { $size, $weight, theme } = props;
  const { fonts, media } = theme.sanity;
  const { family, sizes, weights } = fonts[fontKey];
  const fontWeight = $weight && weights[$weight] || weights.regular;
  const defaultSize = sizes[2];
  const base = {
    position: "relative",
    fontFamily: family,
    fontWeight,
    padding: "1px 0",
    margin: 0,
    "&:not([hidden])": {
      display: "block"
    }
  };
  const resp = (0, import_helpers.responsive)(media, (0, import_helpers.getResponsiveProp)($size), (sizeIndex) => fontSize(sizes[sizeIndex] || defaultSize));
  return [base, ...resp];
}
function fontSize(size) {
  const negHeight = size.ascenderHeight + size.descenderHeight;
  const capHeight = size.lineHeight - negHeight;
  return {
    fontSize: (0, import_helpers.rem)(size.fontSize),
    lineHeight: (0, import_helpers.rem)(size.lineHeight),
    letterSpacing: (0, import_helpers.rem)(size.letterSpacing),
    transform: `translateY(${(0, import_helpers.rem)(size.descenderHeight)})`,
    "&:before": {
      marginTop: `calc(${(0, import_helpers.rem)(0 - negHeight)} - 1px)`
    },
    "&:after": {
      marginBottom: "-1px"
    },
    "& [data-sanity-icon]": {
      fontSize: (0, import_helpers.rem)(size.iconSize),
      margin: (0, import_helpers.rem)((capHeight - size.iconSize) / 2)
    }
  };
}
